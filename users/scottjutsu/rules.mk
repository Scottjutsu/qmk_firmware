SRC += scottjutsu.c 

# RGB_MATRIX_ENABLE ?= no
# ifneq ($(strip $(RGB_MATRIX_ENABLE)), no)
#     SRC += rgb_matrix.c
# endif

# ifeq ($(strip $(OLED_DRIVER_ENABLE)), yes)
#     SRC += oled.c
# endif
